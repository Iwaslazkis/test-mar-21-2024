import 'react'

export type AveryDropdownProps = {
    disabled: boolean,
    options: object,
    defaultOption: string,
    bold: boolean,
    fontSize: number,
    padding: number,
    borderRadius: number,
    color: string,
    backgroundColor: string,
    }